import { Canvas } from "../../src/libraries/canvas.js";

const main = document.getElementById('main');

//-- create canvas
const canvas = new Canvas(400, 400, 'canvasContainer', 'canvas');
canvas.clearCanvasWithColor('red');

//-- Mouse Location on canvas

canvas.addEventLister(onmouseenter) = (event) => {
  
}